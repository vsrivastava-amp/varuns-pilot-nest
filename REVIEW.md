# Review Gateway — DEPRECATED 2026-07-27 (Varun-directed, in-chat)

**Do not append entries here.** Outbound drafts now live as bare files in `review/` — one file per draft, exactly what gets pasted/executed. Conventions in `review/README.md`. Context for a draft lives in the creating session's `runs/` entry; dispositions happen in-chat with Varun; delete the file once disposed (git history keeps everything).

Pending drafts at deprecation were migrated to `review/`; stale entries were retired (list in `runs/2026-07-27-morning-routine.md`). This file's full final state is in git history (commit 647e75e and earlier). Steward: delete this tombstone at next weekly compaction once no session references it.
