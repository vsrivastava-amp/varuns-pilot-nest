# Morning digest — 2026-08-07 (Friday)

*Generated 10:35–11:05 EDT (clock verified via `date`; all times ET). **Two-day window**: no digest ran 8/06, so deltas cover from **2026-08-05 10:45 ET** (seam rule, 15 min behind Wednesday's ~11:00 cutoff; Slack ts 1785941100) through now. Coverage: Jira via curl, Gmail, Calendar, Drive, Slack. Slack/Rovo/Datadog needed a `/mcp` handshake mid-run (Gmail/Calendar/Drive were already up, so the account was right — this was the ordinary un-handshaken state, not the 8/05 wrong-account failure).*

## Attention flags

1. **The contract doc's §2 endpoint is dead, and it has been generated into client code.** The doc records `POST /v1/intent/pciv`, dev host `dev-online-pciv-service.ric1.admarketplace.net`. Varun's own 8/05 17:05 end-to-end validation established the opposite: route `/v1/intent/online-civ` on `dev-online-civ-service.ric1.admarketplace.net`, with the **old hostname NXDOMAIN and old resources pruned**. Alexandr Gontarev has frozen the doc into `github.com/admarketplace-gh/online-pciv-spec` tag **v0.2.0**, and stated the working model in `#online-civ-service` (8/05 14:32): *"Both sides generate from tags: your team the Python models, we the Jackson client."* Yaarit asked Varun to confirm §2 (doc comment, 8/05 19:53). His **dev integration ETA is Aug 8**.
2. **Three explicit "needs from your side" from Gontarev are unanswered after ~2 days** (`#online-civ-service`, 8/05 14:32): (a) timeout/error behavior — on timeout or extraction failure does SSP continue on query text alone or return no ads, confirmed *for all failure classes*; (b) null-omission — spec v0.2.0 assumes unset fields are omitted, service today does `model_dump()` with no `exclude_none` and sends explicit nulls both directions (Yaarit confirmed in a doc comment); (c) per-environment hosts + path confirm + latency profile / rate limits.
3. **Saksham's direct question to Varun is open since 8/05 16:11.** On item 4, testing as close to prod as possible: *"share your concerns here which may be blocking you from understanding the ETA."* Varun's own prod ETA in that same thread is **Monday Aug 10** — one working day after today.
4. **The Bedrock capacity number Varun was waiting for arrived.** Yaarit relayed Zhe Huang in `#online-civ-service` 8/06 19:38: *"For QWANT AI, avg daily requests around 100K in US, 1.6M in FR."* Caveat: that is a Qwant-AI aggregate and online CIV is France **AI Chat only**. 1.6M/day averages ~18.5 RPS, close to Varun's ghost-measured Flash+Chat total (16.7 + 1.2 RPS p50), so it very likely includes Flash. The Chat slice is what answers Saksham's actual question: *"Are there any costs or other concerns that prevent us from telling AWS that the traffic would be a lot more?"*
5. **INFRA-3507 and INFRA-3508 have sat 41 hours with no response** — Not Started, **unassigned**, zero comments ever. Filed by Varun 8/05 17:13 and linked to AI-1538 at 17:14 (that link is the only change on AI-1538 in the whole window). These carry the stage/prod akeyless paths + `wi-online-civ-service` SA, and the bedrock-mantle interface endpoint + Route 53 resolver fix. Not INFRA hygiene — this is the stage/prod rollout unclaimed, against an Aug 10 prod ETA.
6. **Antonio's INFRA-3474 re-test is ~3 days old and untouched.** Last comment is still his 8/04 10:40 "Could you check again?". The Luna evaluation stays behind it.
7. **The experimentation config key now has two conflicting definitions** — see the dedicated section below and `needs-human.md` Q-2026-08-07-01. Varun's service reads this key.
8. **Vespa: the "too many filters" theory is dead; price is the cost.** Roberto's per-filter run (8/06 16:48) plus Aliaksandr's brand measurement (AI-1656) both landed. Dhaval's call this morning: ship the price and GPC-ID changes before any more testing.
9. **Calendar**: standup 11:00–11:30 and R&Y Sprint Demo 13:00–14:00 today, both `needsAction`. New invite: Saksham 1:1 Tue Aug 11 13:30–13:45, `needsAction`. Sprint Retro (Thu 8/06) was canceled; Dhaval canceled Wednesday's 1:1 ("Sorry need the time back today").
10. **Eight `review/` drafts pending, oldest 7/28.**

## New channel: `#online-civ-service` (C0BP2EL5HMW)

Created 8/05 11:22 by Saksham; now the primary channel for this stream and **not previously in the sweep list** (added to the playbook). Charter message (8/05 11:38): 2s E2E latency budget, external commitment **Aug 24**, AI Chat / France only. It carries the whole ETA negotiation, Gontarev's SSP breakdown, and the Bedrock capacity thread.

SSP-side plan from Gontarev (8/05 14:32), for the record: SSP is a thin pass-through with no extraction logic; contract fixed at spec tag v0.2.0; request mapping gated via the `civExtraction` experimentation config; response mapped pCIV → ASV → DSP; **timeout 700ms working default**, config-driven, to be validated against Varun's latency data; Kafka logging unchanged in phase 1. ETAs: **dev integration Aug 8, prod incl. experiment/gating Aug 12**.

Logging question closed benignly (Saksham → Gontarev, 8/05 16:47): SSP Engine always saves the raw JSON request body as `ad_request`, DSP Engine saves the extracted CIV inside the `asv` field of `ad_selection`. No change needed.

Varun's own commitments in-channel (8/05 11:51): prod online eval service **earliest Monday Aug 10**; dev contract-matched **by that day** (delivered — validated 17:05); items 3 and 4 left "unclear", and Saksham has since pressed on 4.

## The endpoint path and the config key — one stale line, three artifacts

The chain is now traceable end to end:

- **DPR-3420's description** (still live) says the A/B test evaluates *"online pCIV extraction … from llm-evaluator-service's `/v1/intent/pciv` endpoint"*.
- **Gontarev** took the path from there — his doc comment says *"Per DPR-3420 the path is `/v1/intent/pciv` — please confirm and fill in here"*, and his channel post lists *"path `/v1/intent/pciv` taken from DPR-3420 — confirm"*.
- **Doc §2** was then filled in with that path and the pre-rename dev host, *after* the rename went live.
- **`openapi.yaml` v0.2.0** generates from the doc, and both teams generate clients from the tag.

**The config key is a separate, verified conflict.** DPR-3420's Scope field (`customfield_11292`) reads: *"Add new configuration type called ~~pCIV Extraction~~ CIV Extraction in the experimentation platform UI in Amplify **(key: pcivExtraction)**"* — only the display name is struck through; **the key was never changed**, and Yaarit's instruction to Arman was "All the rest stays the same" (c171873, 8/05 11:06), consistent with the key standing. But the contract doc §3.2 now documents the field as `experimentContext.config.civExtraction`, with Yaarit's comment explaining she *"renamed pcivExtraction → civExtraction to match DPR-3420"*. The ticket summary was also renamed to `…Experimentation Platform:civExtraction`.

So the doc says `civExtraction`, the ticket's key line says `pcivExtraction`, and the config **shipped**: Arman drove DPR-3420 Not Started → Done by 8/06 09:40, and **RELEASE-6169 is Released**. What actually went into Amplify is not determinable from the ticket. Filed as Q-2026-08-07-01.

*(This also corrects the 8/05 digest, which recorded doc and ticket as agreeing on `pcivExtraction`.)*

## Contract doc — other open comments addressed to Varun

Read fresh from Drive at 10:42 today. Yaarit resolved several of Gontarev's comments on 8/05 (price-direction mapping, `qt`/`prompt` naming, flat `errorMessage` instead of a structured error object, `experimentContext` documented, trailing comma). Still open, and two name Varun directly:

- **§3.1 `queries` array** — Gontarev: *"consider stating what happens to oversized batches (a max, or an explicit 4xx) — currently undefined. Not blocking for us (we send one query per request)."* Yaarit: *"@vsrivastava any suggestions?"*
- **§2 Endpoint** — *"@vsrivastava please confirm"* (flag 1).
- **Null-omission** — Yaarit: *"Still open on our side — the service currently does `response.model_dump()` with no `exclude_none` … Haven't made the code change yet."* This is a service-code decision.
- **Max lengths** — Gontarev suggests bounding (Discover uses 4,096 for prompt, 32,768 for response); Yaarit asked back whether Discover's bound makes ours redundant. Unanswered.
- **ISO 4217** — `priceCurrency` is free-form in both prompt and schema, no validation; the LLM infers it. Yaarit offered to add validation if SSP wants it.
- **AS-13435 "Open API Spec"** (Gontarev, In Review) is the ticket carrying `openapi.yaml`. Varun neither authored nor reviewed it. Pinkel's 8/05 "Do we have a defined contract yet?" on AS-13437 was answered by pointing at it (c171933, 8/05 16:45) — so the question the 8/05 digest tracked as open was closed, but with the GitHub spec rather than the doc §2 blank.

## Vespa / latency

- **Roberto Simoes' per-filter results** (`#proj-scale-serving-and-tracking`, 8/06 16:48, production-like targets from Qwant search terms, 30 RPS): categories ~60ms P95, brand ~20ms, age group ~24ms, price ~50ms with the highest CPU; price combined with other filters ~90–130ms; **four or more filters still ~65–88ms**. Conclusion: filter *count* does not explain the slow queries — specific query characteristics or data distribution do. This is the follow-through on the 8/05 finding that his clean 478-query profile came from low-filter pCIV-derived queries. Ticket **AS-13535** filed this morning.
- **AI-1656 measured "remove the brand filter" and it lost badly** (Aliaksandr Ikonnikau, c171963, 8/06 08:54, VSS stage, cache off, 322 rows): brandAsFilter vs brandInQuery — avg 6.0 → 43.2ms, P95 13 → 71ms, P99 24 → 83ms. His reading: filters run *before* vector search (post-filter-threshold = 1), shrinking the candidate set. His follow-up c171978 (8/06 10:40) is the actionable one and **nobody has replied**: *"some fields that take part in filtering (countryCode, language, currencyCode etc) have no fast-search attribute, what can cause slow filtering."*
- **Dhaval's direction this morning** (10:10, in Roberto's thread): launch the price and GPC-ID changes before further testing. Oren surfaced an easy win — remove `startDateSalePriceEffectiveDate`/`endDateSalePriceEffectiveDate` from queries — and Dhaval told Bhupesh to ship it ahead of the effectivePrice work.
- **Methodology note worth carrying into Varun's own latency work** (Oren, 10:28–10:40): one slow query queues the fast ones in the content cluster, so a client can see >500ms on a query that searches in <10ms, with no signal that queueing is the cause. Watch `Match executor queue size (max)` under the Vespa console query tab; once it climbs you have hit max QPS and further load testing tells you nothing. Roberto pushed back that search latency and HTTP latency would then diverge; Oren agreed and said it is worth clarifying with Vespa.
- **AI-1583** escalated to Munjal's team and gained a second optimization: Bhupesh's c171986 (8/06 11:51) asks for the GPC L1/L2 exact-match replacement **plus** a new `effectivePrice` field replacing separate `price`/`salePrice` filtering. Schema in c172002 (8/06 14:14): `googleProductCategoryLevel1Id`, `googleProductCategoryLevel2Id` (long), `effectivePrice` (double), all `attribute: fast-search`, deliberately no `rank:filter`. Now assigned to **Dhaval Shah** under AI-1637.
- **AI-1598** deferral confirmed on-ticket (Dhaval c171914, 8/05 15:54): Oren stays on the Vespa throughput project through August.
- **AI-1386** — still zero activity. Varun's `ad_request.amp_datacenter_id` hedge is now **7 days** unacknowledged; Q-2026-07-31-01 stays open and unowned.

## Qwant program

- **External call is today.** Agenda (Claire Conklin, 8/06 14:21): new PLIDs timeline, **response handling logic ownership**, A/B testing logic. Dhaval's steer (8/05 16:43): start with open items for the 8/24 launch including feedback on Qwant's latest zip file, **push to increase timeouts**, treat A/B testing as post-launch. The ownership item sits next to Varun's `responseContent` handling.
- **Ghost-mode traffic is roughly what go-live looks like.** Saksham asked (8/05 16:33) whether Aug 24 brings more traffic or new placements; Dhaval: "Mostly yes… maybe an increase if they are only doing 25% today, I am unclear on that piece"; Amarachi: "Should be most of the traffic (for those markets)… I don't think we're being throttled."
- **Text-ad flags went live in prod for two more Qwant Detailed Answers placements this morning** — DBR-6139, Shiva Kumar c172063 (8/07 10:29): `INSERT INTO amp.FEATURE_FLAG`, feature ids 1018/1019 against entity ids **5369 and 5370**, Prod_Aurora, SUCCESS. The 8/05 digest recorded 5357/5359 as the France-AI-mode pair, so these are additional.
- **AI-1620** — still zero comments ever; summary now reads "AMP Discover 3.0 API - online CIV extraction". Direction continues to live in fields only.
- **AI-1542** — no new comments since 8/03.
- Pub-onboarding chain PUB-7979 → PUB-7986 closed out 8/05 13:28 (Tech AM launch, external reporting, ad-ops QA, SE QA all Done).

## Eval datasets

- **AI-1601** — Artem posted his final query (c171951, 8/06 03:11) against `dev_amplify.qwantai_testing_data.top_10k_query_us_amp_requests_v3`, `commercial = true and targets is not null`. Matches what the 8/05 digest predicted; no action left for Varun.
- **AI-1602** (Artem, In Review) — Discover 3.0 merged-ads scoring, sweeping `kvssScoreLinearA/B` over 2,000 `qwant_targets.csv` rows against stage-ric1 AAS, mapping each row onto `asv.intentContext.pciv.targets`. Six A/B pairs, ~1,210 of 2,000 rows returned ads, **avg latency 201.9–208.8 ms across every pair**; text-ad share swings 78.4% → 87.7% with the offset. Proposed for experiment: A 1.10, B −0.25. He asked Amarachi to review and help pick placements (`#proj-amp-discover-3-0`, 8/07 04:00) and posted the same ask to `#team-relevance-yield`. **This runs on Varun's pCIV targets**, so the latency figures are worth knowing.
- **AI-1556** — no new comments.

## Embedding A/B

- **AI-1657 and AI-1426 are both Done and in prod.** Yaarit opened cd-deploy-configs PR #7267 (elme-yield prod-ric1 image 1.0.75 → 1.0.79, bundled with Bhupesh's `activeDeadlineSeconds: 6900` port), filed **RELEASE-6163**, now Released. The 8/05 digest had AI-1657 In Review.
- **AI-1659 is a new unowned gate.** "Route Baseten Models Using the Model Registry" (Joseph Deferio, 8/05 11:42, Not Started, **unassigned**, parent AI-1070): the Baseten path "selects between two configured endpoints using a hard-coded `gte_amp` alias check", and the change "is required before a new Baseten-only model can be enabled through the registry." That reads as a gate in front of using Disha's model `wl1y9l1q` from AI-1653, and nobody owns it.
- **AI-1651 / AI-1653** — zero new comments.
- **Varun/Yaarit DM thread** (8/05 16:51 → 8/06 12:37): confirmed `admarketplace/gte-finetune-12-03-2025` uses CLS pooling, corroborated in the dbx model registry tables. Yaarit: "that's separate from quantization." Varun: "ok well good everythings consistent." Closed.

## Alerts and other

- **AD-9057** (Home Depot Appliances click drop) closed **Done** 8/05 13:13, along with AD-9036/AD-9037 — the 8/05 digest flagged it Open. A new one replaced it: **AD-9099 "CLICK SPIKE: Pandora IT / Qwant UP 2900.0%"** (James Palestri, 8/06 16:00, **Open**).
- **AI-1597** reversed direction. The 8/05 digest recorded the fix as landing in the Kafka producer with VFS needing no change. Tribikram now owns it and c172054 (8/07 08:36) re-gates it: *"before we ask them to make the change, I think we need to confirm from the Business team (or from the leadership) if those missing fields should actually be present in VESPA."* He also adds `asin` to the missing-field list. The fix now waits on a business answer nobody has.
- **AI-1138 "Real-time GPT extraction v1.0"** was moved to Done and Dhaval objected in the same minute (c171915, 8/05 15:56): *"Agreed not a priority for H2. I don't think Done is the right status though. Either rejected or backlog would be better."* Still Done.
- **Feature-store CI churn in Varun's inbox** (not obviously his): the stage jobs `feature-store-cadence-gap-scan-pipeline` and `feature-store-1h-pipeline` were **deleted** by a service principal 8/07 03:25 UTC, a GitHub deployment review for `admarketplace-gh/feature-store` stage has been **waiting for approval** since 03:27 UTC, and a cadence-gap-scan run then failed at 06:11 UTC. Separately Rama hit a stage feature-store failure (`relation "entities" does not exist`) in `#team-relevance-yield` 8/06 — Steven traced it to a schema swap away from `public` and pushed a new build; Rama said this morning 10:27 he is out at a doctor appointment and will update on the CTR inference service release.
- **Housekeeping in Varun's inbox**: an overdue Rippling harassment-training task and a KnowBe4 security-awareness enrollment (8/06).
- **Yaarit asked for pager-duty cover** for Thursday (level 1 R&Y, level 2 ELME) — now moot.

## Bulk noise

168 issues touched in the two-day window; 40 created. Five proximity clusters, all comment-checked, none carrying real activity for Varun's streams: 13 AS epics 8/05 15:03:59–15:04:01 (Pinkel/Ankit H2 roadmap groom, 11 of 13 zero-comment, AS-13529 Rejected in the same pass); 16 DPR issues 8/07 05:40:28–05:42:48 (Saro/Steven external-dashboard QA sign-off, 15 of 16 zero-comment); 8 DPR-3370 tickets 8/06 12:01–12:04 (advertiser-dashboard V2 grooming); 4 DPR brand-logo tickets 8/05 11:11–11:12. Tarun Chadalla's 14 comments across DPR-3386/3413/3416 are real work but entirely outside Varun's streams.

The Varun-centric sweep returned 4 issues and **zero** new comments — no new @mentions of him anywhere in Jira in the window. Every direct ask in this digest came from Slack, Google Docs, or email.
