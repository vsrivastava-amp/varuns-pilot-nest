# Needs Sudo

Pre-staged work requiring elevation, executed when a sudo window opens. Each entry: date, what, exact commands ready to paste, why user-space won't work.

## Backlog

- 2026-07-21 — Inventory last month's actual sudo usage (`log show` / shell history) to see what can move to user-space (venvs, rootless docker, user systemd). *(This inventory itself may not need sudo.)*

---

*(no staged commands yet)*
