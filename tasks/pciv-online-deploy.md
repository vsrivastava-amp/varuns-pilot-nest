# Online pCIV deployment checklist (AI-1538) — multi-session tracker

Started 2026-07-23. Owner: Varun (agent-assisted). Requirements are volatile — re-read `log/pciv-online-service.md` (esp. latest dated entries) before each session. Update checkboxes + add dated notes in place; this file is the cross-session state.

**Division of labor (2026-07-23, Varun↔Saksham):** Yaarit = input/output format + prompt optimization (orthogonal, don't block on it). Varun = get a new llm-eval-service deployment live in base form: dev now, stage/prod soon; Bedrock wired with *a* model (not the best model); rate limits worked with infra.

## Phase A — new dev deployment ("seems live")

Mechanism (verified 2026-07-23, see `playbooks/k8s-cd.md`): ArgoCD; `cd-deploy-configs` repo, **branch `master` = dev+stage overlays, branch `prod` = prod**; auto-sync is ON (`selfHeal: true`) — a merge to master deploys immediately. Application registration = yaml in `cd-releases` `dev/ric1/` (precedent: llm-evaluator-service's Application yamls live on unmerged branches `AI-1371`/`AI-1371-prod` — infra presumably applied them; confirm process with infra).

- [x] **App identity DECIDED 2026-07-24 (Varun in-chat): `online-pciv-service`** (his words: "something like online-pciv"; `-service` suffix added for repo convention — flag if unwanted). Own Datadog service tag, same Docker image.
- [~] **cd-deploy-configs — overlay BUILT 2026-07-24** on branch `AI-1538-online-pciv` (worktree `~/Documents/cd-deploy-configs-online-pciv`, commit 089e5a77a; push pending `review/2026-07-24-ai1538-branch-pushes.txt`): `apps/online-pciv-service/{base,dev-ric1}`, kustomize-build validated. Ingress `dev-online-pciv-service.ric1.admarketplace.net`. **Image tag = unpullable placeholder** — set the `feat-online-pciv` CI tag before PR/merge. 2026-07-28: CI (`bitbucket-pipelines.yml`) runs the full build incl. docker push on **every** branch (`default:` pipeline), tag = `<pyproject version>.<BITBUCKET_BUILD_NUMBER>-<branch>` — so an image for `feat-online-pciv` should exist since Varun's 7/24 push; look up the build number in Bitbucket Pipelines UI (or registry) to get the exact tag. Original item:: create `apps/<app>/base/` + `apps/<app>/dev-ric1/` by copying `apps/llm-evaluator-service/{base,dev-ric1}` (source of copy: `git show origin/master:apps/llm-evaluator-service/dev-ric1/<file>`). Edit: app name/labels, `tags.datadoghq.com/service`, ingress hostname `dev-<app>.ric1.admarketplace.net` (ingressClassName `llm-eval`), config.conf (`DEPLOYMENT_ENVIRONMENT=dev`, DBX host dbc-562d27e2-d74d for now), image tag = current dev tag (1.0.266-* era; check latest).
- [~] **Secrets/identity: reuse WORKS de facto (2026-07-28 — pods started, DBX creds resolved, eval succeeded); formal blessing pending INFRA-3475** (filed 7/28, Not Started as of 7/29 09:00 PT; companion INFRA-3474 = Bedrock perms, also Not Started; Pun DM'd by Varun, responded, work not picked up yet). **2026-07-29 ~07:28 PT: BOTH tickets assigned to Antonio Flores Perez** (i_afperez) — still Not Started, zero comments, but picked up onto someone's plate. If infra says no-reuse: new akeyless paths under `online-pciv-service/dev/ric1/` + new SA ⇒ overlay update + INFRA-3474 retarget.
- [~] **cd-releases — Application yaml WRITTEN 2026-07-24, pushed 2026-07-28.** RESOLVED 2026-07-28 (repo evidence, see playbook): **no infra apply step exists — merging the yaml to cd-releases master via normal PR registers the app** (dev-llm-evaluator-service.yaml is ON master via PR #871; the AI-1371 branch copies were stale). Our yaml diff-matches the master precedent exactly (names/paths only). Remaining: open the PR (after image tag lands in the overlay) → Varun merges. Deregister later = move to DECOM/.
- [x] **Smoke GREEN 2026-07-28 — DEV IS LIVE** (image `1.0.292-feat-online-pciv`, both PRs + bump PR merged by Varun same day): `/health` 200 in 15ms + eval 601 SUCCESS 2.5s w/ 94% cacheRead, probed in-network via one-time DBX job on ML-TEAM-CLUSTER (`/Workspace/Users/vsrivastava@admarketplace.com/scratch/ai1538_dev_health_check.py` — rerunnable; serverless can't reach *.ric1, classic clusters can; agent shells can't at all, VPN-gated). Eval 601 success ⇒ akeyless reuse works de facto. ~~Still unchecked: Datadog service visibility under the new name.~~ **2026-07-29: `online-pciv-service` confirmed in the Datadog service catalog** (services search, read-only MCP) — Phase A fully closed.
- [ ] Guardrail note: prepare branches + PRs; **merges to master auto-deploy** — Varun merges (agent PRs go through `review/` drafts first).
- [ ] Housekeeping (post-merge): the three `~/Documents/*-online-pciv` dirs are **linked git worktrees** of the main clones (made so checkouts don't yank branches from under concurrent sessions sharing the main clones). They do NOT auto-clean — after the PRs merge, `git worktree remove` each + delete merged branches. (Explained to Varun 2026-07-28.)

## Phase B — Bedrock provider with *a* model

- [x] ~~gpt-5-nano not on Bedrock~~ **CORRECTED 2026-07-23 (Varun + online research)**: no nano/mini variants, but closed-weight **GPT-5.4 / 5.5 / 5.6 Sol-Terra-Luna ARE on Bedrock** via the **Bedrock Mantle** endpoint (`https://bedrock-mantle.us-east-1.api.aws/openai/v1`, OpenAI **Responses API only** — no Converse/InvokeModel/ChatCompletions; Bedrock API key auth; in-region us-east-1; **prompt caching w/ 90% cached-input discount**; OpenAI first-party pricing; standard tier only). My catalog sweep missed them because `list-foundation-models` + the price book only cover the bedrock-runtime surface — three gating layers: account grants / region / **API surface**.
- [ ] **Early-testing model recommendation (re-tempered 2026-07-23, Varun)**: Luna is the nano-*role* of the 5.6 family but **NOT nano-priced — $1/$6 per 1M vs gpt-5.4-nano's $0.20/$1.25 (5× both sides, OpenAI list)**; may be slower too (unmeasured). So: **early functional testing = DBX path with gpt-5-4-nano (evalId 2) — zero new code, cheapest**. Bedrock wiring is model-agnostic; Luna (evalId 6 on DBX already) is merely the cheapest *closed-weight OpenAI option on Bedrock/Mantle*, and whether in-network Bedrock-Luna beats out-of-network DBX-nano on latency at 5× cost is an AI-1540/1542 measurement, not an assumption. Converse-path alternates: `gpt-oss-20b` / `ministral-3b`.
- [~] **Service code (dev branch on llm-evaluator-service)** — 2026-07-24: path (b) **BUILT** on branch `feat-online-pciv` (local worktree `~/Documents/llm-evaluator-service-online-pciv`, commit a7949d5, push pending Varun approval — Bitbucket is behavioral-read-only): new domain `domains/pciv_online` (exact copy of civ_extraction, mounted `/v1/intent/pciv`) + `bedrock` provider in `llm/utils/providers.py` via `ChatBedrockConverse` (default boto3 chain: AWS_PROFILE locally, IRSA in k8s) + eval configs 101/102/103 (see log 2026-07-24). Gotcha fixed: invokers sent OpenAI-style `cache_control` content blocks — Bedrock Converse rejects unknown content keys; now gated to ChatOpenAI. Path (a) `bedrock-mantle` (Responses API for GPT-5.x) still TODO. models.json `us.` prefix rule handled at smoke-test time (script self-resolves).
- [~] **Mantle auth — mechanism RESOLVED 2026-07-28** (AWS docs api-keys.html): **short-term Bedrock API keys derive from the current IAM principal** via `aws-bedrock-token-generator` (pip; `provide_token()` caches/auto-refreshes, ≤12h, inherits the principal's permissions) — **so IRSA IS the root identity in-pod**; no long-term key in akeyless needed (long-term = IAM user, AWS says exploration-only). Gotcha: Mantle bearer calls are gated by a **separate IAM action `bedrock-mantle:CallWithBearerToken`** (not `bedrock:CallWithBearerToken`) — must be on the pod role. Laptop test path: SSO login → `provide_token()` → OpenAI SDK with `OPENAI_BASE_URL=https://bedrock-mantle.us-east-1.api.aws/openai/v1`. Remaining: verify account-level access to Mantle/OpenAI closed-weight models with one smoke call (whether a console model-access grant is also needed is undocumented — smoke answers it).
- [ ] **Local test**: run service locally with SSO creds (`aws sso login --profile dev`), civ eval config pointed at the Bedrock model, `scripts/civ_api_accuracy.py` smoke (few rows) — NOTE: Bedrock invocations cost money; Varun pre-approved ~$50 exploration budget? **Not yet — get explicit go before real runs** (his 7/23 instruction).
- [ ] **k8s AWS auth (infra dependency)**: pod needs bedrock:InvokeModel + bedrock:Converse* on us-east-1 — IRSA role bound to the app's service account (dev EKS is in us-east-1, account for dev EKS TBD — confirm whether cluster account = 564079877134 where Bedrock access is verified). Infra ticket.
- [x] New eval config in civ domain (or new domain later, per Yaarit's format work) — **2026-07-24: new domain `pciv_online` built instead** (Varun's direction): evals 101 (nano + offline civ prompt, exact copy of civ eval 2), 102 (nano + conversation pciv prompt from pciv-demo-service `dev-taxonomy-full-l2` c139471, `max_group_size: 1`), 103 (bedrock ministral-3-8b + pciv prompt). **Eval IDs ≥101 reserved for this domain** — DynamoDB cache key is sha256(eval_id, query), no domain component. Tight timeout + `bypassCache` semantics still open (see optimization list, log 2026-07-24).
- [x] **Bedrock smoke call — GREEN 2026-07-24** (Varun logged in live): eval 103 end-to-end SUCCESS — `mistral.ministral-3-8b-instruct` exact id confirmed (ON_DEMAND, us-east-1, no `us.` prefix needed), conversation pCIV extracted with targets[] + GPC paths resolved, **~1.2s laptop→Bedrock** (4230 in / 152 out tokens, no caching — as expected for Ministral). One transient: first call wrapped JSON in fences/prose → lenient outermost-object parse added (commit 50049ed). Rerun anytime: `AWS_PROFILE=dev PYTHONPATH=src/main/python .venv/bin/python scripts/pciv_online_smoke.py --eval-id 103`.

## Phase C — Bedrock rate limits (research + infra ask)

Mechanism (researched 2026-07-23, free API): **AWS Service Quotas, per-account × per-region × per-model**, separate **requests/min** and **tokens/min** quotas; `Adjustable: true` ⇒ raise via `aws service-quotas request-service-quota-increase`; `false` ⇒ AWS support case. Current dev-account (564079877134) us-east-1 defaults, pulled 7/23:
- Ministral 3B/8B: **10,000 RPM / 100M TPM** — covers 200 QPS (12k RPM is close; TPM fine: 200 QPS × 3.6k tok ≈ 43M TPM)
- Qwen3-32B: 10,000 RPM class
- Llama 4 Maverick/Scout (cross-region): **800 RPM / 600k TPM** — way under need (600k TPM ≈ 2.7 QPS at 3.6k tok!); TPM adjustable=true, RPM adjustable=false ⇒ support case
- Nova Micro: 4M TPM on-demand / 8M cross-region
**Deep-dive memo received 2026-07-23** (online research agent; verbatim: `state/bedrock-capacity-memo-20260723.md`). Headlines: 200 QPS needs 12k RPM / ~46M TPM, AWS wants 2–3× headroom forecast (24–36k RPM); **no published turnaround SLA or ceiling — submit asks immediately, involve the AWS account team, treat unapproved capacity as unavailable**; priority/standard/flex **share one quota pool** (priority = scheduling + "up to 25%" output-TPS, no P99 guarantee); Provisioned Throughput doesn't cover our OSS candidates (dead end for now; Reserved tier is the newer separate-capacity option); cross-region = capacity/overflow, not latency (can't pin destination — keep primary path single-region); quotas refresh on 60s cycles and **reserve `input + max_tokens` at admission ⇒ set tight max_tokens** (~400, not 4096/10k); 429=quota vs 503=capacity semantics; **caching correction: GPT-5.6 (Mantle) + Gemma 4 implicit also cached**, still nothing for Llama/Ministral/Mistral. Launch gates: quota/latency/throttling/fallback/cost — fold into AI-1542 acceptance criteria.

- [x] Rate-limit mechanism research (memo above closes it; Mantle quota increases go via AWS Support, not Service Quotas workflow)
- [ ] Re-pull quotas for the finalist model once AI-1540 picks; compute need = target QPS × 60 × tokens/req (use measured prompt tokens per `token-counting` skill); include the 2–3× headroom framing + memo's "strong request" field list in the ask
- [ ] Which AWS account do stage/prod EKS pods run in? Quotas are per-account — the increase must land in the serving account, not the laptop-dev one.
- [ ] **Quota gate check for Ministral at 200 QPS: 10k RPM default < 12k needed** — even the friendliest OSS quota needs an increase before launch-capable; Llama 4 Maverick (800 RPM) is out of the question without account-team action.
- [ ] Draft the infra/AWS ask (numbers + justification per memo's field list) → `review/` (precedent: DBX rate-limit ask 2026-07-23, in REVIEW.md git history). Kick off EARLY — no SLA on approvals.
- [ ] Client-side throttling design for the service: request+token limiters smoothed to RPM/60 & TPM/60, bounded queue, one deadline-aware retry max, shed-don't-amplify (memo §5 list → service requirements)

## Phase C2 — finalist testing in dev (Varun priority 2026-07-28, after screen)

Finalists (Varun-picked off the Pareto): **Gemma 4 31B, GPT-5.6 Luna, Qwen3-Next-80B, Ministral 14B.**

- [x] **Model × provider test matrix wired** (2026-07-28, commits 7b03dbc + 4754b0a on feat-online-pciv, PUSH PENDING): eval config = one (model, surface, prompt) combo, so provider A/B is just two eval ids — 604 qwen80/runtime vs **612 qwen80/mantle**, 607 ministral14/runtime vs **613 ministral14/mantle**, 609 gemma4/mantle, 610 luna/mantle (both Mantle-ONLY — AWS doesn't offer them on runtime; verified via model cards + live probes). Mantle open-weight root is `/v1/chat/completions` (NOT `/openai/v1`), qwen needs `-instruct` id suffix there. All 6 smoke green.
- [ ] ~~**Dev soak driver = Databricks job**~~ **DE-SCOPED 2026-07-29 (Varun in-chat): skeptical a sustained-rate soak rig is needed — general latency tests suffice for the Aug-1 ballparks.** Keep: DBX-driven test batches from the dev workspace POSTing to `https://dev-online-pciv-service.ric1.admarketplace.net` per eval id (classic cluster only), p50/p95/p99 per eval id, real queries (AI-1556 US 50k set landed 7/29: `dev_amplify.qwantai_testing_data.us_queries_gpc_lvl_3`; FR + conversation-shaped still pending). Drop: scheduled/sustained-load machinery. Mantle ramp / Gemma cache steady-state observations = nice-to-have within a longer batch, not a rig requirement.
- [ ] Blocked on dev deployment = the two INFRA tickets + image tag + PR merge (Phase A). Subagent studying INFRA board conventions 2026-07-28; drafts to be revised to house style.
- [ ] Prompt-prefix discipline note for Yaarit (standing constraint): caching (Mantle implicit / OpenAI-style) only hits on byte-identical prefixes ≥1024 tok — keep fixed instruction+taxonomy block first, dynamic content (query/SERP context) last; per-placement prompts each cache independently. (Explained to Varun 2026-07-28.)
- [ ] Verify whether Gemma 4's implicit cache discounts PRICE or only latency: check AWS Cost Explorer line items for `google.gemma-4-31b` after 2026-07-28's ~$25 screen run (read-only, SSO session). Screen assumed full price (conservative).

## Phase D — stage/prod (~~later~~ PROMOTED 2026-07-29 — see priority reframe in session log: MVP ready on prod is the build target)

- [ ] stage overlay on master (`stage-ric1`, DBX host 8321), prod overlay on `prod` branch via release PR (pattern: RELEASE-#### tickets, e.g. 586f23a96)
- [ ] prod akeyless paths + SA (infra), prod ingress via pub-nlb? (check — demo used INFRA-3421)
- [ ] AS-13402-style timeout wiring for the SSP→service hop once the integration ticket exists

## Session log

- 2026-07-23 — File created; mechanism research done (branches, argo, quota model, precedent yamls). Nothing deployed yet. Next concrete step: Varun confirms app name + whether to reuse akeyless/SA, then cut the cd-deploy-configs branch.
- 2026-07-24 — **pciv_online domain built** (Varun's next-steps list): branch `feat-online-pciv` in worktree `~/Documents/llm-evaluator-service-online-pciv` (kept out of the shared clone's tree — another session has untracked work on `feat-civ-eval-luna-gemini`). No prior online-pciv branch existed (checked all origin branches). 476 tests pass (74 new); app boots with `/v1/intent/pciv`. Blocked: (1) Bedrock smoke needs SSO login, (2) push needs Varun (`review/2026-07-24-ai1538-branch-pushes.txt`). Optimization candidates for AI-1542 → log 2026-07-24. Next: smoke call → correct model id → Mantle path (a) → Phase A app-name decision still pending.
- 2026-07-24 (later) — SSO login by Varun → **Bedrock smoke GREEN** (eval 103, ministral-3-8b, ~1.2s laptop; lenient-parse fix 50049ed). `feat-online-pciv` **pushed to Bitbucket by Varun**. **App name decided: `online-pciv-service`.** Phase A artifacts built + kustomize-validated on local branches `AI-1538-online-pciv` (cd-deploy-configs 089e5a77a + cd-releases). Budget memo drafted (`review/2026-07-24-bedrock-test-budget.txt`). 1,000-row real FR query sample pulled for Amarachi (`review/qwant_fr_queries_sample_20260724.{csv,sql}`, git-untracked).
- 2026-07-27 — AI-1568 rejected (wording not pre-approved; process fixed) and folded into **AI-1542 comment 170926** (Varun-approved STE-100 next-steps/footgun). No trusted latency target — ballparks due to business **~Fri Aug 1**. Critical path: budget memo ✅ → soak runs. Also pending: deployment branch pushes, CI image tag, infra confirmations (Application apply, akeyless/SA reuse, Bedrock IRSA).
- 2026-07-28 — **Priority reset (Varun in-chat): e2e latency of the live path FROM DEV is the goal; skip local/DBX soak runs — Bedrock in-network is the point.** Critical path = branch pushes → image tag → PR/merge → dev live, PLUS the IRSA infra ask ASAP. **Model reset: Varun unhappy with Ministral 8B as the lead (too small); wants gpt-5-nano/-5.4-nano — RE-VERIFIED 2026-07-28 (AWS model-cards page): no nano/mini on Bedrock on any surface, OpenAI-direct/Azure only.** Nearest Bedrock analog = GPT-5.6 Luna (`openai.gpt-5.6-luna`, Mantle, launched 7/13, in-region us-east-1, Responses API, standard tier only, prompt caching) at **~5× nano's price** ($1/$6 vs $0.20/$1.25 — reconfirmed fresh). Open decision (Varun/Dhaval, AI-1540-shaped): accept Luna's 5×, or fallback (gpt-oss-20b/120b Converse-surface no-cache, Nova Micro/Lite cached, Qwen3, Ministral demoted to baseline). Access check = one Mantle smoke call after SSO (grants unknown only for Mantle; Converse-surface grants verified broad 7/23).
- 2026-07-28 (later) — **BRANCHES PUSHED (Varun-approved in-chat; draft disposed) + MANTLE ACCESS CONFIRMED + shortlist smokes ALL GREEN** (dev account 564079877134, laptop, ~6 tiny calls, <$0.01): **GLM 4.7 Flash** via plain SigV4 Converse `zai.glm-4.7-flash` 247ms; **Luna** via Mantle Responses `openai.gpt-5.6-luna` 574ms (usage shows cache_write_tokens — caching live); **Gemma 4** via Mantle `google.gemma-4-31b` (NO `-it` suffix) 14s cold → 398ms → 2.7s (⚠️ high variance; Mantle throughput "scales over time, ramp gradually" per docs, and Gemma 4 quotas are NOT in Service Quotas). **No model-access grant needed for Mantle** — short-term bearer via `aws-bedrock-token-generator` off PowerUser SSO just worked; conditional "model access" infra ticket is dead. **Runtime-catalog finding:** the Feb-2026 Mantle cohort is ALSO on bedrock-runtime as ON_DEMAND (zai.glm-4.7-flash, qwen.qwen3-32b-v1:0, qwen.qwen3-next-80b-a3b, deepseek.v3.2, moonshotai.kimi-k2.5, mistral.devstral-2-123b, zai.glm-5, gemma-3-*) — existing ChatBedrockConverse path covers GLM/Qwen today; only Luna + Gemma 4 need the Mantle provider path. **Gotchas:** GPT-5.6 reasoning.effort vocab = none/low/medium/high/xhigh (`minimal` → 400; UNSET → reasoning burn, 60s+ hang on small max_output_tokens — always set effort explicitly); no GET /models on Mantle (404) — use model cards; Gemma 4 caches implicitly but showed cache_write_tokens: 0 (verify caching behavior at real prompt sizes in the soak). Smoke scripts: scratchpad `mantle_smoke.py` (session 3f03f639).
- 2026-07-28 (later) — **Mantle roster correction (Varun's console find, verified via AWS announcements): Project Mantle now serves open-weight models too** — DeepSeek V3.2, MiniMax M2.1, GLM 4.7 + 4.7 Flash, Kimi K2.5, Qwen3 Coder Next (Feb 2026), Gemma 4 family (June 2026); bedrock-mantle speaks Responses + ChatCompletions + Anthropic Messages APIs (per-model support varies — GPT-5.6 is Responses-only). **Prior-art ticket found: AI-1236** (Varun, Done, Apr 2026, ARES accuracy): gpt-5.4-nano 71.5% product/70% keyword ≈ the bar; GPT-OSS med-reasoning 75%/65.3% but ~2× token blowup from reasoning output; Qwen3-Next-80B 73%/66%. → OSS ≈ nano within a few points on ARES-class tasks; reasoning-token overhead is the hidden tax. **Candidate shortlist for dev soak (Varun's marketplace sweep × 7/23 price pull @3.6k-tok prompt+250 out):** Gemma 4 31B ($0.10/$0.35, ~$450/1M req, ONLY open model with implicit caching → prefill-latency win; cached-token *price* discount unconfirmed), GLM 4.7 Flash ($0.07/$0.40, $354/1M req, cheapest), Qwen3-32B ($0.15/$0.60, $693), gpt-oss-20b ($329 but AI-1236 reasoning-blowup caveat — test at low/minimal reasoning), + Luna as cached closed-weight comparator, Ministral stays as wired baseline. **Dropped:** Devstral 2 123B (coding-tuned, wrong task), DeepSeek 3.1/v3.2 (~$2.7k/1M req) + Kimi K2.5 (~$2.9k) (both ~8× the cheap band, not nano-price-point), Qwen3-235B (size/price), Gemma 3 27B (superseded by Gemma 4), Qwen3-Next-80B (no standard on-demand rate in 7/23 price book — flex/priority only; recheck if wanted).

- 2026-07-28 — **DEPLOY + SCREEN DAY (full detail in `runs/2026-07-28-ai1538-catchup.md`).** Dev deployment LIVE end-to-end (Phase A essentially closed). 11-model golden-set screen done (~$25): results `state/pciv-model-screen-20260728.md`, Pareto artifact https://claude.ai/code/artifact/5fe85587-f335-485e-b154-23fa850b36b5. Varun picked finalists **Gemma 4 31B / GPT-5.6 Luna / Qwen3-Next-80B / Ministral 14B**, wired dual-surface (runtime+mantle) as evals 604/612, 607/613, 609, 610 — all smoke green from laptop. INFRA-3474 (Bedrock perms) + INFRA-3475 (secrets/SA) filed, linked; Pun DM'd.
- 2026-07-29 (later) — **Fresh-facts sweep (Jira + Datadog, read-only).** (1) **INFRA-3474 + INFRA-3475 assigned to Antonio Flores Perez ~07:28 PT** — still Not Started, zero comments; separately Pun's DM reply gave Varun the infra contact to reach out to (Varun in-chat — assumed to be Antonio since the assignment landed the same morning; correct here if Pun named someone else). (2) **Datadog: `online-pciv-service` present in the service catalog** — last Phase A check closed. (3) **AI-1556: Bhupesh posted a 50k US query cohort today** (5×10k random `ad_request` samples, GPC-tagged via offline CIV → `dev_amplify.qwantai_testing_data.us_queries_gpc_lvl_3`, 1,041 distinct GPCs, good diversity) — usable soak/screen input NOW; French + conversation-shaped sets still pending. (4) AI-1542 scope narrowed (Varun comment 171029, 7/28): that ticket = ballpark latency only; optimization becomes a separate ticket. (5) **Yaarit models DM sent by Varun (in-chat 7/29) — draft disposed**; AI-1538 status-comment draft (`review/2026-07-29-ai1538-status-jira.txt`) still parked awaiting his call.
- 2026-07-29 (later) — **PRIORITY REFRAME (Varun in-chat) + infra contact live.** (1) Antonio outreach verified in DM (D07T50CEFCM): Varun pinged 07:29 PT per Pun's pointer, Antonio 07:36 PT: "yes i will work on these tickets" — INFRA-3474/3475 committed, expected movement today. (2) **Reframe: the goal remains ballpark latency numbers (Aug 1), but the build target is a minimum viable service that can integrate with SSP and is working + ready to go on PROD.** Parallel lanes: SSP owns the ad-request flow task; Yaarit owns the exact API contract. Our lane = everything in between — dev numbers are a milestone, prod-readiness is the deliverable. Consequences: Phase D promoted from "later"; sustained-rate soak rig de-scoped in favor of general latency tests (Phase C2 note); Yaarit's contract work will reshape the pciv_online domain I/O when it lands — watch that seam, don't over-invest in the current request/response shape.
- 2026-07-29 — Session close-out. **State: waiting on infra** (both tickets Not Started, unassigned, no comments as of 09:00 PT; Pun responded in DM only). **Next session start here:** (1) if INFRA-3474 done → run finalist matrix from dev (evals 601/604/607/609/610/612/613) via DBX job; (2) build the sustained-rate soak notebook (seed: `scratch/ai1538_dev_health_check.py` in dev workspace; classic cluster only, e.g. ML-TEAM-CLUSTER 1203-175656-xk3t1e83); (3) rerun screen at the ~3.6k pciv prompt for finalists (evals in pciv_online domain shape) — Aug 1 ballpark deadline; (4) status-comment draft for AI-1538 parked in `review/2026-07-29-ai1538-status-jira.txt`; (5) housekeeping: worktrees + merged branches removable; check Datadog for the new service tag; Gemma cache-price check via Cost Explorer.

## Handoff brief — Bedrock rate-limit deep-dive (paste into an online Claude session)

> Hi! Context: adMarketplace is building a low-latency LLM extraction service (~100–200 QPS sustained, ~3.6k input + ~250 output tokens/request, P99 < 2s) on AWS Bedrock us-east-1, launching late Aug 2026. We've already pulled current Service Quotas (per-account/per-region/per-model, separate requests-per-min and tokens-per-min; e.g. Ministral 3B/8B = 10k RPM / 100M TPM; Llama 4 Maverick cross-region = 800 RPM / 600k TPM, TPM adjustable but RPM not). Please research from current AWS docs/announcements:
> 1. How Bedrock on-demand quota increases actually work in 2026: service-quotas API vs support case, typical turnaround, what justification AWS wants, hard ceilings people have hit.
> 2. Priority vs standard vs flex service tiers: do they have separate quotas? How is priority latency guaranteed?
> 3. Provisioned Throughput: current pricing model/commitment terms, when it beats on-demand for ~200 QPS sustained, which models support it (esp. open-source ones), latency benefits.
> 4. Cross-region inference profiles: how quotas apply (per profile vs per underlying region), latency implications of cross-region routing for a us-east-1 caller (we measured ~370–900ms round-trips for tiny calls).
> 5. Burst behavior/throttling semantics: token-bucket details, 429 handling best practice, and whether quota is enforced per-second or per-minute (matters for spiky ad traffic).
> 6. Any prompt-caching roadmap for open-source models on Bedrock (currently Nova/Anthropic only) — cost and prefill-latency implications.
> Deliverable: short memo with sources/dates; we'll fold it into our repo. Numbers we quote get re-verified against the quotas API before any infra ask.

- 2026-07-23 — Brief prepared; local mechanism research done (see Phase C). Online-session memo → fold results back into Phase C and `playbooks/llm-eval-system.md` §Bedrock.
