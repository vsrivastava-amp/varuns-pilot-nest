# 2026-08-07 — pciv-contract-sync session

- 2026-08-07 (~11:00 EDT) — Context: Varun in live contract-alignment meeting (Yaarit + Alexandr Gontarev); API contract source of truth = the Google doc (Varun's statement this session). Briefed him from nest state (digest 8/07 flags 1–3, contract-doc-updates list).
- 2026-08-07 (~11:05 EDT) — **Yaarit's branch fetched + pulled locally on Varun's ask**: `feat-online-pciv-qwant-prompt` now local branch tracking origin at `0433751` (worktree `~/Documents/llm-evaluator-service-online-pciv`; was 5804fd3 at the 8/05 merge). Three new commits, all on the OLD `pciv_online` paths (branched pre-rename, so the next merge into feat-online-pciv carries pciv_online→online_civ path mapping plus real content conflicts with our c07929d contract alignment):
  - `4971ab1` (8/03): drops type/funnel-stage from prompts+schemas (resolves the pcivType open question — dropped, not documented); `topic` now retained even when commercial:false (contract §4.3 currently says all-null on false).
  - `ce198f0` (8/05 17:09): **drops the 5-target cap** (our alignment enforces 5 server-side); **GPC returned as raw ID string ("187") not taxonomy path** (contract §4.3 says path strings are authoritative — SSP only needs the ID per her message); **flat `errorMessage` string replaces error{code,message}** ("per agreed SSP contract shape" — matches doc §4.1 example, inverts what our code + doc §4.2/§5 say); **source.items[] = {title, description, rank, url}, snippets field removed** — commit says "match real Qwant prod payloads: there is no snippets field".
  - `0433751` (8/05 19:24): prompt prose says CIV not pCIV; drops qwant prompt header line.
- 2026-08-07 — **⚠️ Source conflict surfaced in-chat (peripheral to the fetch task, hot for the meeting): the snippets removal contradicts our 8/04 measured ghost-stream payloads** (log 2026-08-04: snippets[] on ~78% of flash requests / 93% of chat web_search requests, avg 2.5–4 items carrying them). Her commit claims prod payloads have no snippets. Both can't be right for the same surface — possibly she sampled Chat-bare rows or a different window. Varun to resolve in the meeting; file to needs-human.md if it comes back unsettled.
- 2026-08-07 — No merge attempted — awaiting meeting outcome; deploy hold on the overlay bump unchanged.
- 2026-08-07 (~11:30 EDT) — **Meeting wrapped; spec PR opened on Varun's direction: https://github.com/admarketplace-gh/online-pciv-spec/pull/1 (branch v0.3.0-flat-single-query, 844cb0d), Yaarit + Gontarev to approve.** Source of truth ruling from Varun: the gdoc (11oVe4pk68Dg3qjmlJfuMYcScaweLwrxSpBMI02Ak1_Y) — read fresh with comments; NB the doc's own header still links the spec repo as generation source (Gontarev's open comment proposes repo-as-source; unresolved tension, benign while both stay in sync). Doc's 8/07 revision: FLAT SINGLE-QUERY shape (queries[] gone both directions), placementId camelCase, adRequestId int64 required, prompt≤4096/responseContent≤32768, source.items={title,description,rank,url} NO snippets (Yaarit's position won — the contract now excludes them regardless of the 8/04 stream measurement), targets uncapped, GPC IDs as strings, flat errorMessage, null-omission. experimentContext REMOVED from request (gating lives SSP-side per DPR-3420). v0.3.0 changes + carried AMBIGUOUS markers all in the PR body/CHANGELOG.
- 2026-08-07 — **Deliberate deviation from the doc, flagged in PR + in-chat: spec says `/v1/intent/online-civ` + dev-online-civ-service host** (doc §2 still carries the stale DPR-3420 `/v1/intent/pciv` + old host, which is NXDOMAIN since 8/05; §2 comment still awaits Varun). Doc §2 edit owed by Varun; §4.2 table (`queries[].` prefixes) + §5 (207 row, batch wording, explicit nulls in example) also drifted from the flat shape — doc cleanup list for Yaarit/Varun.
- 2026-08-07 — Service-side gap analysis vs v0.3.0 (for the next iteration on feat-online-pciv, AFTER PR approval): drop queries[] envelope both directions (biggest change; kills 207 path), drop experimentContext, adRequestId int64 already fine, add length bounds, drop snippets from source model, un-cap targets (we enforce 5 server-side today), GPC IDs-not-paths (Yaarit's ce198f0 has this + uncap + flat errorMessage + no-snippets — merge her 3 new commits first), null-omission serialization (exclude_none — still unimplemented anywhere), topic-on-noncommercial + prompt-echo + failure-HTTP-status ambiguities need explicit calls.
