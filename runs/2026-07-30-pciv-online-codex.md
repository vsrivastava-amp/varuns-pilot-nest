2026-07-30 — Session: resumed `tasks/pciv-online-deploy.md`; claim commit `d9360da` pushed to nest main.
2026-07-30 — Startup: nest synced before concurrent edits appeared; reviewed queue, pending `review/` drafts, pCIV map/log, k8s CD, Databricks, Bitbucket, and llm-eval playbooks. Preserved unrelated untracked and concurrent-session files.
2026-07-30 — Varun supplied the post-requirements-fix image tag: `1.0.294-feat-online-pciv` for llm-evaluator-service commit `4c0cfe9`.
2026-07-30 — Refreshed cd-deploy-configs `origin/master` to `257f3650a`; created isolated linked worktree `/private/tmp/pciv-cd-deploy-image-294` on branch `AI-1538-image-1.0.294`.
2026-07-30 — Changed only `apps/online-pciv-service/dev-ric1/kustomization.yaml`: image `1.0.292-feat-online-pciv` → `1.0.294-feat-online-pciv`, removing the now-stale placeholder comment.
2026-07-30 — Validation: `git diff --check` PASS; `kubectl kustomize apps/online-pciv-service/dev-ric1` PASS and renders the exact 1.0.294 image. Local cd-deploy-configs commit: `5e1510886`.
2026-07-30 — Dev Databricks profile `dbc-562d27e2-d74d` authenticated successfully as `vsrivastava@admarketplace.com`; rollout validation is not auth-blocked.
2026-07-30 — Outbound gate: branch push + PR package parked at `review/2026-07-30-ai1538-dev-image-294.txt`; merge auto-deploys. After merge: health → eval 601 → Mantle evals 609/610/612/613 → deadline latency batches.
2026-07-30 — Varun explicitly approved the branch push in-chat; pushed `AI-1538-image-1.0.294` to Bitbucket successfully. Varun will make the PR. Executed review draft deleted per `review/` disposition convention; git history retains it.
2026-07-30 — Varun merged cd-deploy-configs PR #7243; `origin/master` merge commit `0ce7e70ae` pins `1.0.294-feat-online-pciv`. Datadog confirms health 200 from the exact new version.
2026-07-30 — DBX run `45121900854995` failed before service calls because the AI-1556 table column is `KEYWORD`, not `query`. Retry `221827411378553` completed: eval 601 SUCCESS (1.88 s, 18,183 input tokens, 17,152 cache read); Mantle evals 609/610/612/613 all failed quickly.
2026-07-30 — Hardened `tools/pciv/dev_eval_latency.py`: CIV failure details come from `queries[].error`, not `results[]`; progress output now prints enough normalized error text for IAM diagnosis. Uploaded the corrected harness to the existing DBX scratch path.
2026-07-30 — Final probe `190367068942122` isolated the blocker: HTTP 401 from AWS says assumed role `WI-online-pciv-service-eks-use1-us-east-1` is not authorized for `bedrock-mantle:CreateInference`. Official AWS docs and the managed inference policy require `CreateInference` in addition to `CallWithBearerToken`.
2026-07-30 — Fresh Jira read: INFRA-3474 remains In Progress, assigned to Antonio, with the original request listing only `CallWithBearerToken`. Follow-up comment parked at `review/2026-07-30-infra3474-mantle-create-inference.txt`; stage infra and AI-1538 status drafts corrected to include the new action.
2026-07-30 — Varun explicitly approved posting the INFRA-3474 follow-up and will nudge Antonio on Slack. Jira API auth probe returned 200; comment 171392 posted and read-back verified. Executed review draft deleted; Jira and git history retain the exact content.
2026-07-30 — Antonio replied in INFRA-3474 comment 171433 that the permission was added and asked for validation. DBX run `670945446823863` / task `274921216024749` passed: 609 Gemma 4524ms, 610 Luna 1204ms, 612 Qwen Mantle 1172ms, 613 Ministral Mantle 1881ms. All four returned successful extractions; no 401s; `CreateInference` is proven on the pod role.
2026-07-30 — These four samples used `civ_extraction.txt` at 18,183–23,525 input tokens and zero cache reads. They are functional evidence only. The deadline latency run still needs finalist configs in `pciv_online` using `pciv_extraction.txt` (~3.6k prompt), then n≥50 per eval.
2026-07-30 — Validation reply for Antonio parked at `review/2026-07-30-infra3474-validation-green.txt`; posting requires Varun approval.
2026-07-31 — Varun approved the validation reply. Jira API auth probe returned 200; INFRA-3474 comment 171479 posted and read-back verified. Executed review draft deleted; Jira and git history retain it.
2026-07-31 — Audited the four latency samples after Varun questioned caller locality. Code uses `region: us-east-1` and `bedrock-mantle.us-east-1.api.aws`; AWS model cards say Luna, Gemma 4 31B, Qwen3 Next 80B, and Ministral 14B support In-Region us-east-1 and no Geo/Global inference ID. The live topology was DBX → dev service → Mantle; EKS, not DBX, is the Mantle caller. Server `processingMs` was 4465/1187/1155/1860ms versus caller E2E 4524/1204/1172/1881ms, leaving only 59/17/17/21ms in the outer DBX/network/response leg. Conclusion: same-region Mantle benefit was present, but n=1/wrong-prompt numbers remain unsuitable as ballparks. Production-shaped measurement should run from SSP or a temporary same-VPC us-east-1 pod/EC2, use the real ~3.6k prompt and persistent connection, and record caller/server/provider percentiles at n≥50. Direct EC2→Mantle isolates provider time but is not service E2E. Databricks cluster location remained undisclosed in the cluster config and is immaterial to the server-side Mantle leg; AWS dev SSO was expired, so no standalone EC2 probe was launched.
2026-07-31 — After Varun refreshed AWS dev SSO, live account inspection confirmed `eks-dev-use1-01` is in us-east-1 account 564079877134, VPC `vpc-0317d6910f3add39a`, but neither that VPC nor any dev-account VPC has a Bedrock Mantle interface endpoint. AWS service discovery confirms `com.amazonaws.us-east-1.bedrock-mantle` is available as an Interface service in 1a/1b/1d with private DNS `bedrock-mantle.us-east-1.api.aws` and no acceptance requirement. Because the VPC uses company DNS, performed a DNS-only SSM diagnostic on EKS worker `i-0545f7a171eb0da30`; it resolved Mantle to public addresses `3.214.115.45`, `52.87.73.163`, and `34.231.48.123`, ruling out a centralized override. Current pCIV path is therefore OpenAI Luna through Mantle in us-east-1 over the regional public endpoint: target is complete except PrivateLink. Closing dev is an infra-only VPC endpoint/DNS/security-group/policy change; stage/prod need separate endpoints.
2026-07-31 — Varun requested a dev-first INFRA ticket draft for the Mantle PrivateLink gap. Drafted `review/2026-07-31-ai1538-dev-mantle-privatelink-jira.txt` with exact account/cluster/VPC/service identifiers, 1a/1b subnet placement, HTTPS and endpoint-policy requirements, and a custom-DNS footgun: the VPC uses `10.11.128.70/.50`, so infra must verify the pod resolves Mantle to endpoint ENIs rather than assuming the Private DNS checkbox is sufficient. Draft awaits Varun approval before Jira creation.
2026-07-31 — Varun chose to keep the PrivateLink follow-up on Antonio's existing INFRA-3474 rather than create a new ticket. Refreshed INFRA-3474: still In Progress, Antonio assigned, last comment is our successful IAM validation. Replaced the unsent new-ticket draft with `review/2026-07-31-infra3474-mantle-privatelink-comment.txt`; it asks for the same verified dev endpoint/DNS/security/policy work and commits us to DNS plus Luna validation. Posting still requires Varun approval.
2026-07-31 — Varun posted the Mantle PrivateLink follow-up to INFRA-3474 manually. Removed the executed review draft; Jira and git history retain the content. Next validation after Antonio's change: confirm the Mantle hostname resolves to private endpoint ENIs from EKS, rerun Luna, and compare provider/server timing.
2026-07-31 — Audited prompt caching for the 1,204ms Luna sample in DBX run `670945446823863` / task `274921216024749`. The raw result reports `cacheRead=0`, `cache_hit_rate=0.0`, `n=1`, and `warmup=0`; the service maps Responses API `input_token_details.cache_read` into this field. Conclusion: the measured request received no Mantle prompt-cache read benefit. Mantle implicit caching was not disabled, so the cold request may have written a cache entry, but the harness did not retain `cacheCreation` and made no second Luna request that could reuse it. `bypassCache=true` bypassed the service's DynamoDB result cache only, not provider prompt caching.
2026-07-30 — Pending review drafts flagged in chat: pCIV stage infra/stage rollout/status, plus AAS calibration and Sheets GCP-SA drafts; none modified.
