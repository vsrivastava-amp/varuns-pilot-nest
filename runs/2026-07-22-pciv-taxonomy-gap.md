# 2026-07-22 — pCIV demo GPC taxonomy gap (laptop, slug: pciv-taxonomy-gap)

- 2026-07-22 — Task (Varun-directed): get on top of the group-DM thread (Varun/Elisa Branson/Dhaval/Yaarit/Saksham Bhatla, Slack C0BJPQHFFGC) — Qwant flagged `gpc_taxonomy.json` in the external-pciv-demo as non-exhaustive. Cross-referenced Slack thread, org chart, nest playbooks, and both local repos.
- 2026-07-22 — Thread state: Elisa relayed client Q 7/20 ("74 categories in file — exhaustive?"); Dhaval 7/21 asked (a) ensure full list (b) input-token impact vs estimates; Varun 7/22 confirmed oversight (demo=66 L2 in prompt vs 192 total L2; offline service = all ~1400 L3) and committed to full 192 list + prompt modifications. Elisa is replying to client. **Dhaval's token question (b) still unanswered** → draft in REVIEW.md.
- 2026-07-22 — Verified counts from repos (facts, re-derived not trusted from thread):
  - Demo `taxonomy/gpc_taxonomy.json` (all 3 Desktop copies + Documents/pciv-demo-service identical): **75 entries = 19 L1 + 56 L2** (client's "74" ≈ off-by-one). Prompt `prompts/pciv_extraction.txt` "=== GPC ===" section: **66 distinct IDs**. So prompt (66) and json (75) are already out of sync — the model can emit IDs the json resolves but the prompt never offered, and vice versa is the real risk after any edit. Fix must update both together.
  - Full GPC (from `llm-evaluator-service/.../civ_extraction/resources/gpc_taxonomy.json`, 5595 entries): **21 L1 + 192 L2** (+1349 L3). "192 GPCs" in Varun's message = L2 only; a complete demo file at L1+L2 = **213 entries** (demo currently includes L1s, and prompt rule says "L1 if no L2 exists", so keep L1s).
- 2026-07-22 — Token impact (answers Dhaval): demo system prompt today ≈ 2.2k tok total, GPC section ≈ 480 tok. Full L1+L2 in same compact `id:Name|` format ≈ 1245 tok → **delta ≈ +770 tok/request (~+35% system prompt, ≈ 3k total)**. System prompt is a fixed prefix ⇒ provider prefix caching absorbs most of it (offline civ service shows 93–97% cache-read on a 16.4k-tok prompt — see `playbooks/llm-eval-system.md`). Net effective input-cost increase: low single-digit %. The "Key disambiguations" block will also need rework/expansion — not sized here, likely +100–300 tok more.
- 2026-07-22 — Discrepancy flagged (peripheral, not filed as needs-human): Varun's Slack message says demo has "66 total distinct L2 GPCs" — true of the **prompt**; the **json file** the client is looking at has 75 entries (19 L1 + 56 L2). If the client pushes on exact numbers, that's the reconciliation.
- 2026-07-22 — Not done (agent posture on pciv-demo = read-only, per `map/pciv-demo.md`): no edits to demo repo. Implementation task queued; prompt rework has design choices (flat list vs grouped, disambiguation coverage, model regression risk) that need Varun/Yaarit.
- 2026-07-22 (later, ~13:15 EDT) — Dhaval re-pinged the group DM (13:13 EDT, ts 1784740410/1784740418): (1) schedule the taxonomy/prompt fix **asap** — prompts are actively being provided to pubs; (2) Amarachi Miller, Elisa Branson, Cameron Park should update the pub packet with the new prompt; (3) asked for post-update prompt size — already answered by the pending REVIEW.md token draft (~3k tok, +~800). REVIEW.md draft context updated. Implementation task in queue remains blocked on Varun/Yaarit design call, now time-pressured.
- 2026-07-22 (Varun in-session) — REVIEW.md draft revised: leads with post-update prompt size (~3k) + cost estimation, per Dhaval's 13:13 ask.
- 2026-07-22 — Testing-plan brainstorm (internal, not outward) for the taxonomy/prompt expansion. Repo confirmed: `pciv-demo-service` (Bitbucket), local at `~/Documents/pciv-demo-service` — small service (main.py ~298 lines, SSE /api/message), only `smoke_test.py`, no test suite. Sketch:
  1. **Sync invariant test** (must-have, cheap, no server): assert prompt "=== GPC ===" IDs == taxonomy/gpc_taxonomy.json IDs, and json entries (id+name) ⊆ eval-service full taxonomy. Kills the 66-vs-75 drift class permanently; wire into bitbucket-pipelines.
  2. **Output-validity guard**: every GPC ID the model emits must resolve in the json — add assertion to smoke_test.py (this is the live failure mode the current mismatch enables).
  3. **Before/after regression set**: fixed query set (logged Qwant demo traffic if available, else ~30–50 hand-built across verticals) run through old vs new prompt; diff assignments; expect movement only toward newly available categories. Main guard against accuracy loss from a 3× longer list.
  4. **New-category coverage**: synthetic queries per newly added L2 (at least high-traffic verticals) — confirm model picks new IDs over old L1 fallback.
  5. **Disambiguation stress**: test pair(s) per disambiguation rule, expanded alongside the rules.
  6. **Borrow eval harness**: llm-evaluator-service civ_extraction has labeled data at L3 — roll up to L2 as ground truth, measure accuracy old vs new prompt.
  7. **Token/cache verification post-change**: count real prompt tokens (confirm ~3k), watch cache-read rates after deploy.
  Open design points (Varun/Yaarit): flat vs grouped-by-L1 list format; disambiguation scope; regression tolerance threshold.
- 2026-07-22 — Varun sent the cost message to the group DM (in-session confirmation); REVIEW.md disposition updated to sent.
- 2026-07-22 — **Latency-impact test scope** (Varun-directed). Grounded in main.py review:
  - Architecture fact: one generation streams reply → DELIM → pCIV JSON (`generate_events`). User-perceived metrics: TTFT (first `token` SSE), time-to-`reply_done`, time-to-`result`. The +800 prompt tokens hit **prefill only** ⇒ TTFT is the headline metric; output is untouched.
  - Cache reality check: offline service's 93–97% cache-read is batch traffic. The demo is sparse interactive traffic; OpenAI implicit cache TTL ~5–10 min ⇒ cold prefill may be the *common* case for Qwant demo users. So measure cold and warm separately; cold TTFT delta is the honest number.
  - Instrumentation gap: `format_usage()` (main.py:159) drops `input_token_details.cache_read` from langchain usage_metadata — SSE `result.usage` can't confirm cache hits. Fix options: (a) 2-line extend of format_usage (needs repo write → Varun), (b) provider-level harness reads usage directly.
  - Two-level plan:
    1. **Provider-level A/B** (clean, no repo changes): script calls OpenAI directly with old vs new system prompt (same turns), streams, records TTFT + total + usage.cached_tokens. Interleave A/B to control provider variance. N≥50/arm/cache-state, report p50/p95. Models: gpt-5.4-nano (default) + gpt-5.4-mini; optionally mistral-small via OpenRouter (different caching semantics).
    2. **Service-level E2E sanity** (uses existing smoke_test.py SSE parser): two service checkouts (old/new prompts+taxonomy) on two ports, same query set, timestamp SSE events. Catches anything provider-level misses (Quart/streaming interaction).
  - Cache-state control: warm = prime then measure back-to-back; cold = space >10 min or first-of-session. Both prompts (2.2k, 3k) clear OpenAI's 1024-token implicit-cache floor; increments of 128 tok.
  - Second-order watch: expanded disambiguation rules could lengthen/slow the *reply* portion (prompt-design effect, not token-count) — covered by comparing reply_done times + output token counts across arms on the fixed regression query set (reuse test-plan item 3 queries).
  - Acceptance frame (proposal): cold TTFT delta < ~100ms p50 and no p95 regression beyond noise ⇒ report "no material impact"; anything worse → revisit prompt format (e.g., grouped-by-L1 might compress).
- 2026-07-22 (~13:30 EDT) — Saksham pinged Varun+Yaarit in #team-relevance-yield (ts 1784739438.331709, 12:57 EDT) quoting the 66-vs-192 line: (1) prompt-size impact — already sized above (+~800 tok → ~3k); (2) prompt storage — demo repo txt/json + eval-service evalId configs (see `playbooks/llm-eval-system.md`). Reply draft appended to REVIEW.md; the "starts in Google Docs?" clause is unverified, flagged in the draft context.
- 2026-07-22 — Latency harness built (`tools/pciv/latency_harness.py` + queries.txt + README) and validated live against gpt-5.4-nano. Gotcha folded in: gpt-5.4-* rejects `max_tokens` → harness sends `max_completion_tokens` for OpenAI (langchain does this mapping inside the service; raw API callers must do it themselves).
- 2026-07-22 — **Noise-floor baseline run** (A-vs-A, current prompts, n=25/arm/mode, `state/pciv-latency-noise-floor-20260722.jsonl`):
  - Warm (cache-filtered, n=23/arm): TTFT p50 575 vs 622 ms, p95 764 vs 818 ms → **A-vs-A p50 noise ≈ 47ms** with identical prompts. Cache confirmed: 1792/2421 tok cached, 92% hit rate even back-to-back.
  - Cold (nonce cache-bust, cached=0 verified): TTFT p50 573 vs 624 ms, p95 868–1100 ms.
  - **Headline: cold p50 ≈ warm p50 (573 vs 575 ms).** Prefilling the entire 2.4k prompt uncached costs ~nothing at p50 on nano — TTFT is dominated by network/queueing, not prefill. Strong prior that +800 tokens will be invisible; the proposed <100ms acceptance bar sits ~2× above measured A-vs-A noise, keep it.
  - DELIM emission 92–100% per arm (2–4 samples/100 never emitted DELIM or dropped it → reply never terminates into pCIV; worth a look in the regression work, independent of taxonomy change).
  - Real A/B pending new prompts checkout: `--arm-b <new>/prompts`, n=50, mode both.
- 2026-07-23 — Varun set scope: (1) implement fix, (2) regression-test it (error rate + DELIM drop ~unchanged), (3) update bundle to group DM. Ping Varun between 2 and 3 (his instruction). needs: design call w/ Yaarit dropped — Varun directed implementation with minimal-change design (same prompt format, completed list).
- 2026-07-23 — **Fix implemented** on `dev-taxonomy-full-l2` in pciv-demo-service (local commit c139471, not pushed): json 75→213 + prompt list 66→213 IDs, both generated from eval-service full taxonomy via new `tools/pciv/generate_gpc_assets.py` (nest); disambiguations updated where real L2s now exist (building sets→1253, puzzles→3867, board/card games→3793, power stations→127); examples fixed to match; `check_gpc_sync.py` invariant checker added to demo repo (CI-able). Sync check passes: 213 entries, 32 rule refs, 6 example refs consistent. NOTE: full L2 list includes Mature (773:Erotic|780:Weapons) + 25 Business&Industrial L2s — flag to Varun for client-facing pub packet.
- 2026-07-23 — **Token estimate correction**: real tokenizer counts (from API usage): old prompt 2421 → new 3563 = **+~1,140 tok (+47%)**, not +800/~3k as estimated (chars/4 undercounts the ID-heavy list). Warm cache covers it (2816/3563 cached). Bundle message must use corrected numbers — Dhaval was told ~3k/+800 (close-ish but correct it).
- 2026-07-23 — Harness extended into regression harness: grades post-DELIM pCIV exactly as main.py parses it (parse_ok), validates emitted GPC IDs per-arm taxonomy (bad_gpc_ids), captures finish_reason + raw tail on failures. Full A/B running: warm n=200/arm (resolves ~4% DELIM rate ±2.5%), cold n=50/arm → `state/pciv-regression-ab-20260723.jsonl`.
- 2026-07-23 — **Regression A/B verdict: PASS** (`state/pciv-regression-ab-20260723.jsonl`, 499 samples, gpt-5.4-nano; A=old 66-ID prompt, B=new 213-ID):
  - pCIV emission (DELIM): warm 196/197 vs 197/198 (identical); cold 48/50 vs 50/50. Overall A 98.8% / B 99.6% — no regression.
  - Parse failures beyond missing DELIM: zero in both arms. Invalid GPC IDs: A 0, B 1/248 (hallucinated 5360 — not in ANY GPC level — for "weather app" on the weather query; service resolve_gpc handles unknown IDs gracefully → name:null). Watch item, not a blocker.
  - **DELIM-drop root cause found**: all 4 drops are the same query ("how do I fix a leaky kitchen faucet"), all finish_reason=stop — model completes a long how-to reply and skips the output protocol entirely. Behavioral, not truncation, not taxonomy-related; equal in both arms. Fix would be protocol reinforcement for how-to conversations — separate small task.
  - Latency: warm TTFT p50 503 vs 504 ms (dead even), p95 within noise. Cold TTFT p50 549 vs 658 (+108ms, vs ~±50ms A-vs-A noise floor and the ~100ms bar — consistent with small real prefill cost of +1142 uncached tok); cold p95 B *better* (1225 vs 2508). Call: no material impact.
  - Output length: warm mean 330→310 tok. Prompt: 2421→3563 tok measured; warm cache read 2816/3563.
- 2026-07-23 — Status message SENT by Varun to group DM (11:55 EDT, lightly edited: Winston framed as "will help create the new bundle"). Entry retired from REVIEW.md.
- 2026-07-23 — Dhaval follow-up (12:01 EDT): (1) reduce prompt size? Qwant will balk at 3.5k; (2) is 3,563 CIV-only or CIV+glue+pub — cares only about CIV. Measured (tiktoken o200k_base, dev branch): **CIV prompt alone 3,307**; glue 240 (protocol 192 + persona 48). CIV sections: GPC list 1,536 | examples 595 | target rules 394 | disambig 277 | type rules 215 | schema 165 | header 103. Cut candidates measured: B&I 25 subcats −179; Mature −12; examples 5→3 ≈ −230 → CIV ~2,890. Bigger lever: per-pub vertical subsets, cheap now that list is generated. Reply draft: `review/2026-07-23-gpc-prompt-size-reply.txt`. Any accepted cut ⇒ regenerate assets + re-run A/B before ship.
- 2026-07-23 — Prompt-compaction menu expanded (all tiktoken-measured): compact example JSON −181 (also shortens model *outputs* — examples teach format); L1 ids inlined into vertical headers −81; combined with B&I −179, Mature −12, examples 5→3 ⇒ examples total −330 → **CIV ≈ 2,700** (from 3,307). Deliberately NOT cutting: output_protocol's 69-tok stripped-history explanation — it guards against the DELIM-drop imitation failure mode. Reply draft updated (still exec-concise). Prompt structure confirmed 3 parts: chat 48 + protocol 192 + CIV 3,307 (+~16 message wrapper = 3,563 API).
- 2026-07-23 — Compaction candidate BUILT and measured per working-with-models skill (deltas by subtraction on constructed text, o200k_base): header −43, B&I −180, Mature −13, L1-inline −75, disambig prune −151, brand −31, type rules −59, examples 3+compact-JSON −384 ⇒ **CIV 3,307 → 2,371 (−936)**. Earlier eyeballs were off up to 70% (disambig −90 est vs −151, examples −330 vs −384). Candidate: `state/pciv-extraction-compact-candidate-20260723.txt` (NOT on branch). If adopted: regenerate json with B&I/Mature L2 exclusions (generator flag needed — json must shrink with prompt or sync check fails), re-run A/B. Kept: protocol stripped-history guard; gaming-headset multi-gpc rule; used-iPhone condition teaching lost with dropped example — condition rule line still present.
- 2026-07-23 — Varun sent his own prompt-size reply (15:18 EDT, tier-1 cuts, hedged examples) — draft entry retired; measured compact candidate (−936) stays in state/ for when the trim decision lands.
- 2026-07-23 — Elisa (16:51 EDT): OK to tell client L2s? updated json to share? **Sequencing risk flagged**: client counts categories; sharing 213-entry json before Dhaval's trim decision risks a second revision, and json contains Mature (773 Erotic/780 Weapons) — undecided client-facing content call. Reply draft (green-light L2 message, hold file until trim call): `review/2026-07-23-elisa-json-reply.txt`.
- 2026-07-23 — Elisa reply angle changed per Varun: share full L2 json now + explicit as-is-vs-trimmed TBD caveat (not a hold). Draft rewritten; 213-entry json staged at ~/Desktop/gpc_taxonomy_L1_L2.json for attach. Mature-rows-in-client-file note kept in REVIEW entry only.
