# 2026-07-22 — prakash-intent-type — does ML use MySQL ssp.intent_type?

- 2026-07-22 — Trigger: Prakash Patel DM (12:08 EDT, D093GDY36SD): DB team wants to delete/repurpose MySQL `ssp.intent_type` for the new media-segment taxonomy; asks if ML uses it in production.
- 2026-07-22 — Searched all local repos (`~/Documents`) for `ssp.intent_type`, `amp.intent_type`, and any SQL FROM/JOIN/INSERT against an `intent_type` table: **zero hits**. Serving-side repos (dsp-engine, elme-yield*, elme-pricing, ad-auction-service, vertica-elme-retrieval, keyword-vector-search-service) also clean.
- 2026-07-22 — What ML actually reads: `{env}_amplify.intent_clf.intent_type_master` (Databricks-native lookup) — in `elme-offline-intent-classifier` (intent_classification_workflow, llm_intent_classification, product_ads_expansion_workflow) and `qt_intent_type_classification` notebooks. Output tables (`qt_expan_intent`, `pub_report`, `adv_report`) store INTENT_TYPE_ID from that lookup.
- 2026-07-22 — History (Slack + obsidian): brand-taxonomy release RELEASE-5309 (2025-07-09) created MySQL **`amp.INTENT_TYPE`** (note: *amp*, not *ssp* — Prakash's message says ssp; discrepancy flagged in draft). It was meant to replace `intent_type_master` but the swap never happened; Varun's 2025-07-14 note: "not updating intent type master yet" + the two tables have **different ID orderings** (e.g. Brand=3 in master vs Brand Straight=5 in amp.INTENT_TYPE). Varun's 2025-07-16 msg to Chloe confirms ELME/Zhe use `intent_type_master`; "we might move it to mysql" (never done).
- 2026-07-22 — Gap I couldn't close from this laptop: other people's Databricks jobs (Zhe / ELME team-side) aren't in local clones. Draft reply asks Prakash to ping them; per Varun's own July-16 inventory they also use `intent_type_master`, so risk is low.
- 2026-07-22 — Deliverable: draft reply appended to `REVIEW.md` (pending Varun). No outbound sent.
- 2026-07-22 — Varun pushed back on completeness → checked Databricks system tables via dev warehouse (dev auth reads prod_amplify + system). Facts (UTC):
  - `prod_amplify.ssp` has NO intent_type table in Unity Catalog — only `amp.intent_type` + `amp.query_intent_type` (EDM, June 2026). Prakash likely means `amp.INTENT_TYPE`.
  - `amp.intent_type` 1-yr lineage: one recurring programmatic consumer — aguo@'s DBSQL query writing `dev_pricing.pricing_team.supply_card` (4/14, 5/2, 6/2, 7/2 via Jobs, 7/15 manual). Rest = ad-hoc human reads (~monthly, various analysts). prpatel/mthakkar reads 7/21-22 = their own deletion investigation.
  - `amp.query_intent_type`: heavily used daily — `sp_metrics_gold_edm_pipeline` (EDM performance tables) + kji@ scheduled query (11:31 UTC daily) + others. That's Prakash's own EDM stack though.
  - Unresolved reader `78610386105725`: bare TABLE reads of both tables, only 7/21-22, no entity attribution — pattern matches Catalog Explorer browsing during their investigation, not a prod job. Noted, not blocking.
  - Caveat: lineage covers Databricks reads of the replica only; direct MySQL-side consumers (services) are DB team's telemetry to check, not ours.
- 2026-07-22 — Varun decided response shape ("no, we use intent_clf.intent_type_master instead" + pricing heads-up); REVIEW.md draft updated. 👀 reaction added to Prakash's msg (Varun-directed in-chat = approval per guardrail 8).
