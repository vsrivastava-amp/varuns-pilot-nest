# 2026-08-05 — morning routine (fallback path)

- 2026-08-05 10:13 ET session start. Nest already in sync with origin; no other session's uncommitted work in tracked files.
- 2026-08-05 **Every claude.ai MCP connector was dead this session** — Slack, Rovo, Gmail, Calendar, Drive, AND Datadog all returned "MCP server not connected" on first call; ToolSearch subsequently reported their tools removed. No `/mcp` handshake had been run. Fell back per playbook: Jira via curl (credential probed healthy via `/myself`), nest state only. Told Varun in-chat which legs did not run and asked for `/mcp`.
- 2026-08-05 Jira sweep window 2026-08-04 10:25 ET → ~10:20 ET today (15-min backward overlap per seam rule). Broad sweep `project in (AI, INFRA, "AS")` — note `"AS"` must be quoted (reserved JQL word; both first attempts failed with a clear error). 136 issues over 2 pages via `nextPageToken`, `isLast` verified; created-count (18) reconciled against created-list — no gap. `fields=` honored on the curl path, everything stayed inline, no overflow. Also swept DPR (44, mostly Munjal's H2 groom) and cross-project `text ~ Qwant/pciv` (1 hit: AD-9057) and `watcher = currentUser()` (3, all known).
- 2026-08-05 Comment-checked 12 stream issues + 8 quiet anchors + AI-1588/AD-9057 changelogs. Headline findings in `state/digest-2026-08-05.md`: Antonio still waiting on Varun's DNS re-test (INFRA-3474); Pinkel asked for the API contract on AS-13437; Yaarit filed AI-1654 (online pCIV A/B) + DPR-3420 (`pcivExtraction` config); Disha self-served the Baseten deployment (AI-1653) discharging Monday's ask to Varun; Dhaval deferred llm-evaluator scaling to Sept (AI-1598); Bhupesh's GPC-ID plan on AI-1583; AI-1597 root-caused to Kafka producer; RELEASE-6156 Released with `*.di.ampfeed.com` certs (closes 8/04's id/di question); AD-9057 Qwant click-drop alert Open.
- 2026-08-05 Datadog probe for the prod-alerts leg: tool schema loaded via ToolSearch but the call returned "not connected" — same dead state as the rest. Listing-visible ≠ connected (consistent with the 2026-07-27 status note: pre-handshake exposure).
- 2026-08-05 needs-human: appended a dated update to Q-2026-08-03-01 (AI-1654 description = fourth France-in-scope confirmation, still no decision record). No new Q entries — nothing hit the conflict bar.
- 2026-08-05 review/ still holds 8 pending drafts (oldest 7/28); flagged in-chat. The 2026-08-03 AI-1542 ballparks draft looks already executed (posted as c171586 on 8/03) but was never deleted — awaiting Varun's word to remove.
- 2026-08-05 Playbook updates: three gotchas appended to `playbooks/morning-routine.md` (all-connectors-dead fallback experience; `"AS"` reserved-word quoting + curl-sweep shape; listing ≠ connected). JQL specifics belong in `playbooks/jira.md` long-term; kept in morning-routine today alongside the existing 8/04 timezone note so the pending-relocation set stays in one place.
- 2026-08-05 Open for a follow-up session once `/mcp` runs: Slack/Gmail/Calendar/Datadog legs (Fastly thread state with Norbert now OOO, Dhaval's three asks, waiver deadline 8/6, standup verification).
