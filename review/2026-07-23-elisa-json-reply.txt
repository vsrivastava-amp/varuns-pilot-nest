Yes — L2s is the right message: full L2 coverage across all verticals (plus top-level fallbacks).

On the json: hold a day before sharing the file. Dhaval's size question is live and we may trim a couple of verticals from the list (Business & Industrial subcategories, Mature) — the file we hand the client should be the final one. Since this client counts categories, I'd rather send the exact list once than revise it on them a second time. As soon as the trim call is made I'll get you the json same-day.
