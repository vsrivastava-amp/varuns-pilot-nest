Cache refresh is done. The notebook re-hit all 18,071 eval-4 entries on prod with bypassCache (~50 min, concurrency 2). 8,830 keywords came back with product categories. The rest returned no GPC. That is the current model's answer, not an error. Gateway telemetry shows zero rate-limiting in the window. One batch of 200 hit a 503, so those keywords still have the old cache entries. The notebook is idempotent. I will re-run it once to cover them. Thanks for the review and the bypassCache check!
