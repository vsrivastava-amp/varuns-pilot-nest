# AI-1538 dev SA/secrets switch — push + PR (HOT PATH: this is what unlocks
# Bedrock from dev; current pods run as wi-llm-evaluator-service = no Bedrock)

cd ~/Documents/cd-deploy-configs-online-pciv
git push -u origin AI-1538-dev-sa-secrets
# PR: AI-1538-dev-sa-secrets -> master, title:
#   AI-1538: switch online-pciv-service dev to its own SA and akeyless paths
# One commit (91209e752), one file (apps/online-pciv-service/dev-ric1/deployment.yaml):
#   serviceAccountName: wi-llm-evaluator-service -> wi-online-pciv-service
#   akeyless:/llm-evaluator-service/dev/ric1/databricks/* -> akeyless:/online-pciv-service/dev/ric1/databricks/*
# Straight from Antonio's INFRA-3474/3475 comments (171257/171262).
# imagePullSecret unchanged per the same comments. Kustomize-build validated.
#
# Merge = immediate redeploy (ArgoCD auto-sync). Validation after merge, agent-run
# via DBX job (needs `databricks auth login --profile dbc-562d27e2-d74d` first):
#   1. /health 200            — pods came up under the new SA
#   2. eval 601 (DBX path)    — proves the NEW akeyless paths hold real values
#   3. eval 609 or 610        — proves Bedrock/Mantle perms on the new SA
# Then the "Thank you so much!" to Antonio is earned and safe.
