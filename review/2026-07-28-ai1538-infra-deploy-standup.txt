# File as: project INFRA, issue type Task, no labels/components, assignee empty (infra self-assigns). Link AI-1538 + the companion Bedrock-permissions INFRA ticket.

Title: Confirm secrets and service-account setup for online-pciv-service in dev

Description:

We are standing up `online-pciv-service` in dev EKS ric1 (AI-1538) — a separate deployment of the llm-evaluator-service image with its own pods, Datadog service tag, and hostname (dev-online-pciv-service.ric1.admarketplace.net). Same setup pattern as dev-llm-evaluator-service.

The deployment itself rides the normal PR flow (no action needed unless we have this wrong): overlay PR to cd-deploy-configs master (https://bitbucket.org/admarketplace/cd-deploy-configs/src/AI-1538-online-pciv/apps/online-pciv-service/) and Application yaml PR to cd-releases master (https://bitbucket.org/admarketplace/cd-releases/src/AI-1538-online-pciv/dev/ric1/dev-online-pciv-service.yaml — same pattern as dev-llm-evaluator-service.yaml, PR #871).

Two things we do need from infra before merging:

* Can the new app reference llm-evaluator-service's existing akeyless paths `llm-evaluator-service/dev/ric1/databricks/*` and the imagePullSecret `docker-registry-access-token-ampdockercirw`? If reuse isn't allowed, please create paths under `online-pciv-service/dev/ric1/` instead (precedent: DBR-6025 for pciv-demo prod) and we'll update the overlay.
* Should the app reuse the `wi-llm-evaluator-service` workload-identity service account, or get its own? If a new one, tell us the name and we'll update serviceAccountName in the overlay. The companion INFRA ticket attaches Bedrock permissions to whichever SA is chosen.

Thanks!
