# File as: project INFRA, issue type Task, no labels/components, assignee empty (infra self-assigns). Link AI-1538 + the companion Bedrock-permissions INFRA ticket.

Title: Register ArgoCD Application for online-pciv-service in dev (cd-releases)

Description:

Hello! We are standing up `online-pciv-service` in dev EKS ric1 (AI-1538) — a separate deployment of the llm-evaluator-service image with its own pods, Datadog service tag, and hostname (dev-online-pciv-service.ric1.admarketplace.net). Same setup pattern as dev-llm-evaluator-service (AI-1371).

Everything is prepared on branches:

* kustomize overlay: https://bitbucket.org/admarketplace/cd-deploy-configs/src/AI-1538-online-pciv/apps/online-pciv-service/ (PR to master to follow)
* ArgoCD Application yaml: https://bitbucket.org/admarketplace/cd-releases/src/AI-1538-online-pciv/dev/ric1/dev-online-pciv-service.yaml (copied from the AI-1371 precedent)

Asks:

* Please register/apply the ArgoCD Application from the cd-releases branch. With AI-1371 the Application yamls stayed on unmerged branches — let us know if the process has changed.
* Please confirm the new app can reference llm-evaluator-service's existing akeyless paths `llm-evaluator-service/dev/ric1/databricks/*` and the imagePullSecret `docker-registry-access-token-ampdockercirw`. If reuse isn't allowed, please create paths under `online-pciv-service/dev/ric1/` instead (precedent: DBR-6025 for pciv-demo prod) and we'll update the overlay.
* If a new workload-identity service account is preferred over reusing `wi-llm-evaluator-service`, tell us the name and we'll update serviceAccountName in the overlay (the companion INFRA ticket attaches Bedrock permissions to whichever SA is chosen).

Thanks!
