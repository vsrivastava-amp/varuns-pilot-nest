Hey <@U08EU7D9GKC>, Dhaval asked us to merge the AI-1474 Gemini config to main and deploy main to prod, even if future keyword runs stay on dev.

I found a config mismatch in prod. `prod_amplify.llm_evals.civ_config` stops at eval 4, but the deployed service image `1.0.286-main` also defines eval 5 as GPT-5.2 and eval 6 as GPT-5 Mini.

Should we reconcile the service to the table by removing the existing eval 5 and 6 definitions, then add Gemini as eval 5? Or should we preserve those service-only configs and add Gemini under a new ID?

If we reuse eval 5 or 6, should we clear that prod DynamoDB cache namespace first? The cache key does not include the model, and direct API calls could have populated entries even though those IDs are absent from `civ_config`.

The same service PR will revert AI-1361 PR #44. That ticket's discussion held the skip-cache-for-scores-1–5 behavior because it can cause systematic cache misses and token-cost spikes until the prompt also forbids those scores.

Should the Gemini config also get a row in `prod_amplify.llm_evals.civ_config`, and should that row be active or inactive while runs stay on dev?

One capacity check remains. We confirmed the Gemini increase in dev, but not in prod. Do we need to confirm prod ITPM/OTPM before a code-only deployment if no batch run will use prod yet?
