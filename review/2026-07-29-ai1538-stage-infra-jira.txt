# INFRA ticket draft: stage secrets + SA for online-civ-service
# (single ticket; mirrors INFRA-3474+3475 which Antonio completed for dev)
# Suggested title: Create secrets and service account for online-civ-service in stage EKS
# REWRITTEN 2026-08-05: app renamed online-pciv-service -> online-civ-service (uniform
# naming, Varun's call); stage infra is requested under the NEW name from day one.
# Dev keeps its existing wi-online-pciv-service SA/paths — no dev infra change needed.

We are extending our online CIV extraction service (AI-1538, currently live in dev EKS ric1 as `online-pciv-service`, being renamed `online-civ-service`) to stage EKS ric1, same pattern as stage-llm-evaluator-service. The deployment configs ride the normal PR flow; this ticket covers the two infra pieces, matching what INFRA-3474/INFRA-3475 set up for dev:

* Please create akeyless paths `online-civ-service/stage/ric1/databricks/*` (DATABRICKS_ACCOUNT_ID, DATABRICKS_CLIENT_ID, DATABRICKS_CLIENT_SECRET) with the stage Databricks credentials — same values as `llm-evaluator-service/stage/ric1/databricks/*`.
* Please create the workload-identity service account `wi-online-civ-service` in the stage cluster with the same Bedrock IAM policy as the dev counterpart from INFRA-3474 (`wi-online-pciv-service`): bedrock:InvokeModel, bedrock:Converse, bedrock:ConverseStream, bedrock-mantle:CallWithBearerToken, and bedrock-mantle:CreateInference in us-east-1.

We keep using the global imagePullSecret `docker-registry-access-token-ampdockercirw` per INFRA-3475.

Thanks!
