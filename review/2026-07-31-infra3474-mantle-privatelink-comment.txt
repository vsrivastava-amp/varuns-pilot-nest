Hey Antonio, we validated one more gap on this same dev Mantle path. `online-pciv-service` currently reaches `bedrock-mantle.us-east-1.api.aws` through public IP addresses. There is no Bedrock Mantle interface endpoint in dev account `564079877134`.

Could you add `com.amazonaws.us-east-1.bedrock-mantle` to the dev EKS VPC?

* Cluster: `eks-dev-use1-01`
* VPC: `vpc-0317d6910f3add39a`
* Enable Private DNS for `bedrock-mantle.us-east-1.api.aws`.
* Add endpoint ENIs in `subnet-0a3cb949a8af631d2` (`us-east-1a`) and `subnet-05f1dc5d674c61d34` (`us-east-1b`). Mantle does not currently advertise endpoint availability in the cluster's `us-east-1c` AZ.
* Allow HTTPS from the EKS workload path.
* Allow `bedrock-mantle:CallWithBearerToken` and `bedrock-mantle:CreateInference` in the endpoint policy. The workload role already has both IAM permissions.

The VPC uses company DNS servers `10.11.128.70` and `10.11.128.50`. Please make sure EKS resolves the Mantle hostname to the endpoint's private addresses. The custom resolvers may need a forwarding rule in addition to enabling Private DNS.

I will validate DNS and rerun the Luna evaluation after the endpoint is ready. This request is dev-only for now.
