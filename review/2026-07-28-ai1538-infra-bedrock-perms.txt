# File as: project INFRA, issue type Task, no labels/components, assignee empty (infra self-assigns). Link AI-1538.

Title: Add Bedrock invoke permissions to online-pciv-service workload identity in dev EKS

Description:

Hello! We are deploying a new app `online-pciv-service` to dev EKS ric1 (AI-1538, online pCIV extraction for Qwant). It is a second deployment of the llm-evaluator-service image and calls LLMs on AWS Bedrock in us-east-1. App registration is tracked in the companion INFRA ticket (linked).

Please attach an IAM policy to the pod's workload-identity role — service account `wi-llm-evaluator-service` if the new app reuses it, or the new app's SA if one gets created (see companion ticket) — allowing these actions in us-east-1:

* bedrock:InvokeModel
* bedrock:Converse
* bedrock:ConverseStream
* bedrock-mantle:CallWithBearerToken

The last action is separate from the bedrock:* ones: it gates calls to the bedrock-mantle endpoint, which serves the OpenAI and Gemma models. The service mints short-term bearer tokens from the pod role at runtime (aws-bedrock-token-generator), so no stored API keys and no new secrets are needed.

Please also confirm the Bedrock model access agreements for these model IDs are enabled in the AWS account the dev EKS pods run in (we've verified access in account 564079877134 — please confirm that's the same account):

* google.gemma-4-31b (bedrock-mantle)
* openai.gpt-5.6-luna (bedrock-mantle)
* qwen.qwen3-next-80b-a3b (bedrock-runtime) / qwen.qwen3-next-80b-a3b-instruct (bedrock-mantle)
* mistral.ministral-3-14b-instruct (both endpoints)

Thanks!
